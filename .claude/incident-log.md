# Incident Log — HomeTeam

One row per mistake. Append-only. Pattern repeats twice → propose graduating the prevention to a global rule in `~/.claude/rules/agent-behavior.md`.

| Date | Cause | Impact | Fix | Prevention |
|------|-------|--------|-----|------------|
